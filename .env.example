# Passwords for password-gated pages (see scripts/gated-pages.config.mjs).
# Copy this file to .env (gitignored) and fill in real values for local
# builds. In CI, these are set as GitHub Actions repo secrets instead — see
# .github/workflows/deploy.yml.
GATE_MEDITATION_LIBRARY=
